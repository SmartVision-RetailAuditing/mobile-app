import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class AppIcons {
  static const Icon iconTask = Icon(Icons.assignment);
  static const Icon iconMap = Icon(Icons.map_outlined);
  static const Icon iconCamera = Icon(Icons.camera_alt_outlined);
  static const Icon iconDashboard = Icon(Icons.bar_chart);
  static const Icon iconProfile = Icon(Icons.person_outline);
  static const Icon iconUser = Icon(Icons.person, size: 48, color: Colors.white);
  static const Icon iconId = Icon(Icons.badge_outlined);
  static const Icon iconRole = Icon(Icons.work_outline);
  static const Icon iconMail = Icon(Icons.mail_outline);
  static const Icon iconPhone = Icon(Icons.phone_outlined);
  static const Icon iconLogout = Icon(Icons.logout);
}
